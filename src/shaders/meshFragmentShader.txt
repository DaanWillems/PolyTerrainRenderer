#version 330

out vec4 fragColor;
in vec4 pass_colour;
in vec2 outTexCoord;

uniform sampler2D texture_sampler;

void main()
{
    //fragColor = (texture(texture_sampler, outTexCoord)) * vec4(pass_colour, 1.0);
	fragColor = pass_colour;
}
